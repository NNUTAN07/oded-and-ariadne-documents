apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: azure-file-pvc-xls
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: ""  # Explicitly set to empty to avoid default storage class
